# Two Sum

给定整数数组 `nums` 和整数目标值 `target`，找出数组中和为目标值的两个元素，并返回它们的下标。

## 约束

- 每个输入恰好存在一个答案。
- 同一个元素不能重复使用。
- 返回下标的顺序不限。

## 接口

```go
func TwoSum(nums []int, target int) []int
```

## 示例

```text
输入：nums = [2, 7, 11, 15], target = 9
输出：[0, 1]
```

